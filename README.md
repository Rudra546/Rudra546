import React, { useState, useEffect } from "react"; import { motion } from "framer-motion"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Play, Pause } from "lucide-react"; import Confetti from "react-confetti"; import { useWindowSize } from "react-use";

const photos = [ "/mnt/data/file-6ApodBL4HUyrfxEDfaLLkU", "/mnt/data/file-Wed8tWfzUV7ZJBfxsHoHLU", "/mnt/data/file-MbTAFPsWAtU63M8zQz9yTU", "/mnt/data/file-89gbigkFB7Jpg4Zfd5uava", "/mnt/data/file-S8zAAijdsifaaZPjJGXcbA", "/mnt/data/file-XEwCfoHH7h8B74rUv7yutj", "/mnt/data/file-3uAqzeHqht1cUHyLvnyy1W", "/mnt/data/file-58ThzpPnZZwgyvWMmbpCuH", "/mnt/data/file-JUfScLYZ5anAFLQ9vqpZhE", "/mnt/data/file-T5A6VRVyUpo8dFE8CTSARi", ];

const messages = [ "You are my sunshine ☀️", "Every moment with you is special 💖", "You make my heart skip a beat ❤️", "I am the luckiest to have you 😘", "Your smile is my happiness 😊", "Forever isn’t long enough with you 💞", "You are my dream come true ✨", "I love you more than words can say 💓", "With you, every day is magical 🥰", "You are my heart, my soul, my everything ❤️" ];

const BirthdayWish = () => { const { width, height } = useWindowSize(); const [currentPhoto, setCurrentPhoto] = useState(0); const [isPlaying, setIsPlaying] = useState(false); const [showFireworks, setShowFireworks] = useState(false); const audioRef = React.useRef(null);

useEffect(() => { const interval = setInterval(() => { setCurrentPhoto((prev) => { if (prev + 1 === photos.length) setShowFireworks(true); return (prev + 1) % photos.length; }); }, 4000); return () => clearInterval(interval); }, []);

const toggleMusic = () => { if (audioRef.current) { if (isPlaying) { audioRef.current.pause(); } else { audioRef.current.play(); } setIsPlaying(!isPlaying); } };

return ( <div className="flex flex-col items-center justify-center min-h-screen bg-gradient-to-r from-pink-400 via-red-400 to-pink-400 text-white p-5 relative overflow-hidden"> {isPlaying && <Confetti width={width} height={height} numberOfPieces={150} recycle={false} />} {showFireworks && <motion.div className="absolute top-0 left-0 w-full h-full bg-transparent animate-fireworks" />} <h1 className="text-5xl font-bold mb-4 animate-pulse drop-shadow-lg">Happy Birthday My Love ❤️</h1> <Card className="w-96 h-96 flex items-center justify-center shadow-2xl border-red-500 border-4 bg-white relative"> <CardContent className="flex flex-col justify-center items-center w-full h-full relative"> <motion.img key={currentPhoto} src={photos[currentPhoto]} alt="Birthday Memories" className="rounded-2xl w-full h-full object-cover shadow-lg border-4 border-white" initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 1 }} /> <motion.div className="absolute bottom-5 bg-white text-red-500 font-bold px-4 py-2 rounded-xl shadow-lg" initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 1.5 }} > {messages[currentPhoto]} </motion.div> </CardContent> </Card> <Button onClick={toggleMusic} className="mt-6 flex items-center px-6 py-3 bg-red-500 hover:bg-red-700 rounded-full shadow-xl transition-transform transform hover:scale-110"> {isPlaying ? <Pause size={20} className="mr-2" /> : <Play size={20} className="mr-2" />} {isPlaying ? "Pause" : "Play"} Song </Button> <audio ref={audioRef} src="/mnt/data/shayad.mp3" loop /> {showFireworks && <motion.h2 className="text-4xl font-bold mt-6 animate-bounce text-yellow-300">Love You Forever! 💖</motion.h2>} </div> ); };

export default BirthdayWish;

